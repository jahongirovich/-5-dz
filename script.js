const user = {
    name: 'bosit',
    id: '123456',
    password: '123456'
  };
  
  console.log(user);

  const name = prompt("Введите имя:");
const id = prompt("Введите ID:");
const password = prompt("Введите пароль:");

const user1 = {
  name: name,
  id: id,
  password: password
};

console.log(user);
  
